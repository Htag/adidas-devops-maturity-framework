pipeline {
    agent any 
    stages {
        stage('Pipeline initialisation') {
            step {
                sh '''
                echo "Hello"
                '''
            }
        }
    }
}

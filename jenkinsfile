pipeline {
    agent any 
    stages {
        stage('Pipeline initialisation') {
            steps {
                sh '''
                echo "Hello"
                '''
            }
        }
    }
}

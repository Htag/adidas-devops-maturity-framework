properties([pipelineTriggers([githubPush()])])

pipeline {
    agent any 
    stages {
        stage('Pipeline initialisation') {
            sh '''
            echo "Hello"
            '''
        }
    }
}
